@model Medicure_Entity.Physician

@{
    ViewData["Title"] = "Index";
}

    <nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item">
        <a asp-controller="Physician" asp-action="ViewAppointments" asp-route-id="@Model.Id">View Your Appointments</a>
    </li>
    <li class="breadcrumb-item">
        <a asp-controller="Physician" asp-action="GetallDrugs" asp-route-id="@Model.Id">Drug</a>
    </li>
    <li class="breadcrumb-item">
        <a asp-controller="Physician" asp-action="AppointmentHistory" asp-route-id="@Model.Id">Appointment History</a>
    </li>
  </ol>
</nav>
<h1>Hello Physician</h1>
<h2>@Model.Name</h2>

